### 组件维护指南（基础版）

- **目录约定**
  - **schema 类型定义**：`frontend/src/core/schema/types.ts`、`frontend/src/core/schema/basic.ts`（基础组件节点）、`frontend/src/core/schema/chart.ts`（图表节点）
  - **默认配置（default config）**：
    - 基础组件：`frontend/src/config/basic-config/*.config.ts`
    - 图表组件：`frontend/src/config/chart-config/*.config.ts`
  - **属性面板字段注解（hints）**：
    - 注册表：`frontend/src/config/hints/index.ts`
    - 基础组件 hints：`frontend/src/config/hints/basic.ts`
    - 图表组件 hints：`frontend/src/config/hints/chart.ts`
  - **渲染组件（materials）**：
    - 基础组件：`frontend/src/components/materials/basic/*`
    - 图表组件：`frontend/src/components/materials/chart/*`
  - **组件渲染注册表**：`frontend/src/components/materials/registry.ts`
  - **拖拽创建默认节点**：`frontend/src/config/index.ts`（`componentDefaultConfigs` / `getNewComponentConfig`）
  - **组件面板入口（拖拽列表）**：
    - 基础组件：`frontend/src/components/panels/component-panel/BasicPanel.tsx`
    - 图表组件：`frontend/src/components/panels/component-panel/ChartPanel.tsx`
  - **属性面板渲染**：
    - props 表单：`frontend/src/components/panels/property-panel/PropsFormRenderer.tsx`（根据值类型推断控件，并读取 hints 补充标题/枚举/min/max 等）
    - 图表数据源：`frontend/src/components/panels/property-panel/DataSourceEditor.tsx`（仅图表组件显示）

- **新增一个“物料组件”的标准步骤（以基础组件 Clock 为例）**
  - **1. 定义类型（TypeScript 类型层）**
    - 在 `frontend/src/core/schema/types.ts` 中为组件添加 `ComponentType` 枚举值，例如：`"Clock"`。
    - 在 `frontend/src/core/schema/basic.ts` 中新增对应的节点接口（如 `ClockComponent`），并把它加入 `ComponentNode` 联合类型中。
  - **2. 定义默认配置（拖拽创建的 DSL）**
    - 在 `frontend/src/config/basic-config/clock.config.ts` 新增并导出 `clockDefaultConfig`（类型为 `ClockComponent`）。
    - 默认配置需包含：`id`、`type`、`name`、`props`（业务配置）、`style`（尺寸/位置/层级）等字段。
    - 在 `frontend/src/config/index.ts` 把新默认配置加入 `componentDefaultConfigs`，以便拖拽时 `getNewComponentConfig(type)` 能创建实例。
  - **3. 编写渲染组件（真正渲染 UI 的 React 组件）**
    - 在 `frontend/src/components/materials/basic/Clock.tsx` 实现组件。
    - 约定接收 `node`（以及容器组件可选的 `renderChildren`），通过 `node.props` 渲染 UI。
  - **4. 注册到渲染注册表（Renderer 才能按 type 找到它）**
    - 在 `frontend/src/components/materials/registry.ts` 中 `import` 新组件，并注册：
      - `ComponentRegistryManager.register("Clock", Clock as unknown as RegistryComponent);`
  - **5. 配置属性面板字段注解（推荐）**
    - 属性面板使用 `PropsFormRenderer` 自动推断控件类型，但**中文标题/枚举/min/max/placeholder** 等需要在 hints 中补齐。
    - 在 `frontend/src/config/hints/basic.ts` 新增 `clockHints`，并在 `frontend/src/config/hints/index.ts` 挂到 `componentHintsRegistry.Clock`。
  - **6. 在组件面板中暴露拖拽入口（推荐）**
    - 在 `frontend/src/components/panels/component-panel/BasicPanel.tsx` 的 `component` 数组中新增一项（`type/name/icon`）。
    - `type` 必须与 schema 中的 `ComponentType` 完全一致，否则拖拽创建会找不到默认配置或渲染组件。

- **新增一个“图表物料组件”的额外步骤（相对基础组件）**
  - **默认配置位置不同**：新增到 `frontend/src/config/chart-config/*.config.ts`，并同样注册进 `frontend/src/config/index.ts`。
  - **schema 定义位置不同**：图表节点类型通常在 `frontend/src/core/schema/chart.ts` 定义（同时仍需在 `types.ts` 的 `ComponentType` 里加入 type 字符串）。
  - **数据源编辑**：图表类型在属性面板会额外显示 `DataSourceEditor`（见 `frontend/src/components/panels/property-panel/DataSourceEditor.tsx`），如新增图表类型记得同步 `isChartType` 的判定逻辑（如果该文件按枚举/白名单判断）。

- **属性面板联动说明（当前实现）**
  - **组件属性（props）**：`PropsFormRenderer` 会遍历 `node.props`：
    - 根据值类型推断控件（boolean→Switch、number→InputNumber、string→Input/Select/Radio、color→ColorPicker 等）
    - 再从 `componentHintsRegistry[type]` 读取注解，补充中文标题、枚举、min/max、placeholder、隐藏字段等
  - **页面设置**：当选中 `RootContainer` 或切换到“页面设置”tab 时，走 `PageFormRenderer`（见 `frontend/src/components/panels/property-panel/PageFormRenderer.tsx`）
  - **图表数据源**：图表组件会额外显示 `DataSourceEditor`，用于配置静态数据/API/字段映射等

