# Vision-Craft 项目难点亮点总结

## 项目概述

Vision-Craft 是一个 **AI 驱动的可视化大屏搭建平台**，用户通过拖拽或自然语言即可构建数据可视化大屏。  
技术栈：React 18 + TypeScript + Zustand + dnd-kit + react-rnd + ECharts + Express + MongoDB + LangChain + DeepSeek/智谱 AI。

---

## 一、架构设计亮点

### 1. Schema 驱动的组件体系

**核心思想**：一切皆 JSON DSL — 页面由一棵 `PageDSL` 组件树描述，编辑器操作本质是对这棵 JSON 树做 CRUD，渲染器递归遍历树并实例化为真实 React 组件。

**关键实现**：
- `PageDSL` 定义了页面根结构（`type: "RootContainer"`、`settings`、`children`），所有组件节点通过 `ComponentNode` 联合类型约束（28 种组件），每种组件有独立的 TypeScript 接口
- `ComponentRegistryManager` 使用静态类 + `Map` 实现组件注册表，29 个组件在模块加载时预注册，渲染器通过 `type` 字段 O(1) 查找
- `Renderer` 采用递归渲染 + 注册表查找的经典模式，支持编辑态（RndItem 包裹）、预览态（纯绝对定位）两种渲染路径
- 容器组件通过 `renderChildren` 回调引擎来渲染子组件，实现了**控制反转（IoC）**，容器与子组件完全解耦

**相关文件**：`frontend/src/core/schema/`、`frontend/src/core/renderer/Renderer.tsx`、`frontend/src/components/materials/registry.ts`

### 2. 前后端分层清晰的 AI 架构

**前端**负责交互、SSE 通信和实时 Schema 更新：
```
AIChatPanel(UI) → useAIGenerate(Hook) → EventSource(SSE) → 实时写入 schema-store
```

**后端**负责 AI 编排、知识检索和结构化输出：
```
路由(SSE) → Pipeline(编排) → Agent(LangChain) → Tools(28个组件工具) + Cache + AbortGuard
```

两端通过 SSE 事件协议解耦（`start`/`ai_chunk`/`end`/`error`），前端不关心 AI 具体实现，后端不关心 UI 渲染。

### 3. 三层配置驱动的属性面板

属性面板完全**不硬编码**任何组件的表单，而是通过三层配置自动生成：

| 层级 | 文件 | 作用 |
|------|------|------|
| 默认配置 | `config/basic-config/`、`config/chart-config/` | 定义每种组件的完整默认 DSL（含 props、style、dataSource） |
| 属性提示 | `config/hints/` | `FormHint` 元数据注解（中文标签、枚举选项、控件类型覆盖、数值范围） |
| 字段映射槽位 | `config/chart-field-slots.ts` | 声明每种图表需要的 fieldMapping 字段（如 BarChart 需要 x+y） |

`PropsFormRenderer` 遍历 `node.props` 自动推断控件类型（boolean→Switch、number→InputNumber、string→Input），再用 hints 覆盖特殊字段。新增组件只需添加配置，无需修改表单渲染器代码。

---

## 二、前端技术亮点

### 4. 高精度拖拽坐标系转换

从侧边栏拖拽组件到画布需要解决**三重坐标系转换**：

1. **视口坐标 → 画布坐标**：拖拽结束时获取元素视口位置（`activeRect`），减去画布容器的视口位置（`overRect`）
2. **滚动补偿**：画布可滚动，必须加上 `scrollLeft/scrollTop`
3. **缩放还原**：画布有 CSS `transform: scale(zoom)` 缩放，坐标必须除以 `zoom` 还原为原始坐标系

```typescript
// DndWrapper.tsx 核心计算
offsetLeft = (activeRect.left - overRect.left + scrollLeft) / zoom;
offsetTop  = (activeRect.top  - overRect.top  + scrollTop)  / zoom;
```

最终通过 `snapToGrid` 吸附到网格，保证组件精确对齐。

**相关文件**：`frontend/src/core/dnd-kit/DndWrapper.tsx`

### 5. requestAnimationFrame 驱动的参考线高亮防抖

拖拽过程中需要实时显示吸附参考线，但频繁的 `setState` 会导致性能问题。方案：

- 用 `requestAnimationFrame` 替代 `setTimeout`，在每一帧渲染前更新高亮线位置
- 通过 `guideStore.setActiveSnaps` 通知 `GuideLayer` 显示蓝色参考线
- 拖拽结束后清除参考线状态

**优势**：与浏览器渲染帧同步，避免了 setTimeout 的时序不确定性，确保每帧最多更新一次，流畅度显著提升。

**相关文件**：`frontend/src/core/rnd/RndItem.tsx`、`frontend/src/store/guide-store.ts`

### 6. 双模式缩放系统

缩放系统采用 **autoFit + manualZoom 双模式**：

- **自适应模式**（`autoFit=true`）：`fitScale = Math.min(containerWidth/canvasWidth, containerHeight/canvasHeight)`，画布自动缩放适配编辑器可视区域
- **手动模式**：用户可自由缩放（0.1x ~ 3x），关闭自适应后以手动缩放值为准
- **DOM 三层嵌套结构**：外层可滚动容器 → 缩放后尺寸占位层（`scaledWidth/scaledHeight`）→ 实际画布内容层（`transform: scale()`）

外层占位层的关键作用是给浏览器提供正确的滚动区域大小，而实际内容通过 CSS `transform` 进行视觉缩放，二者配合实现了坐标系不变、滚动正确、GPU 加速的缩放体验。

**相关文件**：`frontend/src/hooks/useZoom.ts`、`frontend/src/components/editor/Index.tsx`、`frontend/docs/缩放系统详解.md`

### 7. 标尺与辅助线联动系统

标尺（Ruler）固定在画布滚动容器的上方和左侧，辅助线层（GuideLayer）叠加在画布内容上方：

- **动态刻度密度**：根据缩放级别自动调整刻度间距和标注密度
- **滚动联动**：标尺刻度随画布滚动实时偏移，保证刻度与画布内容位置一一对应
- **吸附参考线高亮**：拖拽组件时，`RndItem` 计算吸附位置并写入 `guideStore`，`GuideLayer` 实时绘制蓝色参考线

**相关文件**：`frontend/src/components/editor/ruler/Ruler.tsx`、`frontend/src/components/editor/ruler/GuideLayer.tsx`

### 8. ECharts 统一封装与数据映射抽象

14 种图表组件统一使用 `EChartsBase` 封装层：

- **实例生命周期管理**：在 `useEffect` 中创建/更新/销毁 echarts 实例
- **自适应尺寸**：通过 `ResizeObserver` 监听容器尺寸变化，自动调用 `resize()`
- **数据映射抽象**：通过 `dataSource.fieldMapping` 的语义化 key（x/y/name/value/series/size/category），不同图表复用相同数据结构，只需改映射关系。每种图表在 `chartFieldSlots` 中声明所需字段

各图表组件只需关心 **DSL → EChartsOption 的转换**，渲染交给 `EChartsBase` 统一处理。

**相关文件**：`frontend/src/components/materials/chart/EChartsBase.tsx`、`frontend/src/config/chart-field-slots.ts`

### 9. 生产者-消费者模式的 AI 精准编辑

选中组件后可在其下方输入自然语言指令精准修改：

```
AiEditInput (生产者) → component-edit-store (桥接) → AIChatPanel (消费者) → AI 后端
```

- `AiEditInput` 将用户指令和目标组件写入 `component-edit-store`
- `AIChatPanel` 监听 `pendingEdit`，消费后构建增强 prompt（注入组件完整 JSON），调用 AI 接口
- AI 返回时通过 Upsert 语义精准更新目标组件（已有 ID 走 `updateItem`，新 ID 走 `addItem`）

该模式实现了 UI 组件与 AI 通信的完全解耦。

**相关文件**：`frontend/src/core/rnd/AiEditInput.tsx`、`frontend/src/store/component-edit-store.ts`、`frontend/src/hooks/useAIGenerate.ts`

### 10. SSE 流式生成与实时画布更新

`useAIGenerate` 通过 EventSource 实现 SSE 流式通信，AI 每生成一个组件就实时推送到画布：

```
EventSource → "ai_chunk" 事件 → 解析 JSON 组件节点 → 实时写入 schema-store → 触发画布重渲染
```

- **Upsert 语义**：如果 AI 返回的组件 ID 已存在于画布，走更新逻辑；否则走新增
- **页面配置与组件分离**：无 `type` 但有 `props/settings` 的 chunk 识别为页面配置更新
- **已有组件清单注入**：每次请求时将画布现有组件信息注入 prompt，让 AI 精准引用已有组件 ID
- **AbortController 取消**：ESC 键或面板关闭时中断 SSE 连接，节省资源

**相关文件**：`frontend/src/hooks/useAIGenerate.ts`、`frontend/src/components/editor/AIChatPanel.tsx`

### 11. 选中态智能翻转与交互细节

`SelectionOverlay` 渲染组件的选中态叠加层（蓝色边框 + 组件名标签 + 操作按钮）：

- **智能翻转**：当组件靠近画布顶部（`style.top < 20`）时，标签和按钮自动翻转到组件下方显示，避免被画布边缘裁剪
- **事件穿透**：`CanvasEmpty` 使用 `pointer-events-none` 确保空状态引导不阻挡画布拖拽
- **中文输入法兼容**：`AiEditInput` 通过 `isComposing` 检测输入法状态，防止回车时误提交

**相关文件**：`frontend/src/core/rnd/SelectionOverlay.tsx`、`frontend/src/core/rnd/AiEditInput.tsx`

### 12. 缩略图并行生成

保存页面时并行生成缩略图，使用 `html-to-image` 将画布 DOM 截图为 base64：

- 缩略图生成设置 800ms 超时保护，避免截图阻塞保存流程
- 截图与保存请求并行发起，不影响保存速度

**相关文件**：`frontend/src/components/editor/Toolbar.tsx`、`frontend/src/utils/thumbnail.tsx`

---

## 三、后端 AI 技术亮点

### 13. LangChain Agent + 结构化工具输出

核心 Agent 基于 LangChain + DeepSeek，采用 **Tool Calling** 模式强制结构化输出：

- **toolStrategy(pageSchema)**：强制模型输出必须符合 Zod 定义的 `pageSchema` 结构，杜绝自由文本输出
- **每组件一工具**：为 28 种组件各定义独立的 LangChain Tool（如 `create_text_component`、`create_bar_chart_component`），每个 Tool 有精确的 Zod Schema 约束输入参数和专属 description，显著降低 LLM 参数错误率
- **componentPaletteTool（组件清单工具）**：Agent 可调用此工具查询当前支持哪些组件类型，辅助决策

**相关文件**：`backend/src/ai/agents/create-agent.ts`、`backend/src/ai/tools/component-tools.ts`、`backend/src/ai/schema.ts`

### 14. 组件生成 Pipeline 的精细编排

`create-pipeline.ts` 编排了从用户输入到组件输出的完整流程：

1. **缓存层**：基于 `userPrompt + mode` 的 SHA256 哈希检查缓存，5 分钟内相同请求直接返回，避免重复 LLM 调用
2. **组件清单注入**：每次请求时将画布已有组件的 `id/type/name` 构建成文本清单注入 User Message，防止 AI 编造不存在的组件 ID
3. **流式输出分类处理**：对 Agent 返回的每个 chunk 精细分类 — `notes` 跳过、`isPageConfig` 走页面配置更新、`isComponent`（白名单校验）走组件节点推送
4. **组件兜底**：自动补充 `id`（nanoid）和默认 `style`，确保前端拿到的组件都能正确渲染
5. **thread_id 路由**：`pageId || sessionId` 作为会话隔离键，同一页面的多次对话共享上下文

**相关文件**：`backend/src/ai/pipelines/create-pipeline.ts`

### 15. AbortGuard 中断控制机制

`createAbortGuard` 封装了 AbortSignal 的中断控制，用于在用户取消请求时安全中断 AI 生成流程：

- `guard.run(asyncFn)` 包裹整个 Pipeline 执行
- `guard.throwIfAborted()` 在关键检查点（如向量检索前后、LLM 调用前）主动检查中断信号
- `guard.dispose()` 在 finally 块中清理资源
- 前端 AbortController 与后端 AbortGuard 联动，ESC 键或页面关闭即可中断正在进行的 LLM 调用

**相关文件**：`backend/src/ai/abort-guard.ts`、`backend/src/ai/pipelines/create-pipeline.ts`

### 16. 对话摘要中间件（SummarizationMiddleware）

Agent 集成了 LangGraph 的 `summarizationMiddleware`：

- 当对话达到 500000 tokens 或 10 条消息时自动触发摘要压缩
- 保留最近 10 条消息，历史消息压缩为摘要
- 解决了多轮对话中上下文窗口溢出和 token 膨胀问题
- 配合 `MemorySaver` checkpointer，通过 `thread_id` 实现会话级别的状态隔离

**相关文件**：`backend/src/ai/agents/create-agent.ts`

### 17. 双模型 RAG 知识问答系统

RAG 系统采用**双模型架构**：

| 模型 | 用途 | 温度 | 理由 |
|------|------|------|------|
| DeepSeek | 组件生成 | 0.5 | 需要一定创意 |
| 智谱 AI | 知识问答 | 0.3 | 需要精准确定性输出 |

**完整 RAG 流程**：
1. **文档加载**（`loader.ts`）：从 `knowledge/docs/` 加载 Markdown 文档，使用 `RecursiveCharacterTextSplitter` 分块（chunkSize=500, chunkOverlap=50），注入 source/title 元数据
2. **向量存储**（`store.ts`）：使用 MongoDB Atlas Vector Search 存储向量，复用业务数据库减少基础设施复杂度；智谱 Embedding 适配（强制 `encodingFormat="float"`，解决 OpenAI SDK v6 默认 base64 不兼容问题）
3. **索引构建**（`indexer.ts`）：支持全量 clean/rebuild，可独立运行（`import.meta.url` 判断直接执行）
4. **问答管道**（`rag-pipeline.ts`）：Top-4 相似度检索 → 上下文格式化（标注参考来源） → LLM 流式生成 → 额外推送引用来源（标题 + 前 100 字符摘要）

**知识库内容**：3 份 Markdown 文档覆盖图表配置指南（286行）、组件 Schema 文档（314行）、使用指南（164行）。

**相关文件**：`backend/src/ai/knowledge/`、`backend/src/ai/pipelines/rag-pipeline.ts`、`backend/src/ai/agents/rag-agent.ts`

### 18. 联网搜索与背景图集成

`integrationTools` 中集成了 Tavily 搜索 API，Agent 可在生成大屏时自动联网搜索背景图片：

- `updatePageConfigTool` 在接收到页面配置中的 `backgroundImage` 字段后，可触发联网搜索获取匹配的图片 URL
- 搜索结果经过 LLM 筛选后写入 `PageDSL.settings.backgroundImage`

**相关文件**：`backend/src/ai/tools/integration-tools.ts`、`backend/src/ai/tools/component-tools.ts`

---

## 四、技术难点

### 难点 1：拖拽坐标系的多重转换

从侧边栏拖拽组件到画布涉及三重坐标系（视口坐标、滚动偏移、缩放比例）的叠加转换。任何一层计算错误都会导致组件落点偏移。难点在于：
- CSS `transform: scale()` 不改变元素的 layout 尺寸，视口坐标与实际坐标存在缩放倍率差异
- 滚动容器内的元素位置需要加上 scrollOffset
- `DraggableItem` 和 `DroppableItem` 分属不同的 DOM 子树，需要通过 `getBoundingClientRect` 统一到视口坐标系后再计算

### 难点 2：SSE 流式生成的生命周期管理

SSE 连接涉及多个异步生命周期交织，容易出现内存泄漏和状态不一致：
- 组件卸载时必须关闭 EventSource，否则后台持续接收数据写入已卸载的 store
- 用户快速连续发送请求时需要取消前一个 SSE 连接（通过 AbortController）
- `useEffect` 中的异步操作需要 `cancelled` 标志位防止竞态
- 后端的 `AbortGuard` 需要在 LLM 调用的各个阶段设置检查点，因为某些操作（如 MongoDB 向量检索）不支持 AbortSignal

### 难点 3：AI 生成可靠性的三层保障

让 LLM 稳定输出符合 Schema 的 JSON 结构是核心挑战：
- **第一层（Zod Schema 约束）**：后端为每个组件 Tool 定义精确的 Zod Schema，从类型层面约束 LLM 输出
- **第二层（白名单校验）**：Pipeline 中对 AI 输出的 `type` 字段进行 `COMPONENT_TYPES` 白名单校验，过滤非法组件类型
- **第三层（兜底补全）**：对缺失 `id` 或 `style` 的组件自动补充默认值，确保前端渲染不崩溃

新增组件类型时需要同步更新多处：Zod Schema、Tool 定义、白名单、前端注册表。

### 难点 4：缩放系统与多子系统的协调

缩放比例改变会影响几乎所有子系统：
- 拖拽坐标需要除以 zoom 还原
- react-rnd 的拖拽和缩放操作需要传入 `scale` 参数
- 标尺刻度密度需要根据缩放级别动态调整
- 参考线位置需要与缩放后的网格对齐
- 自适应模式下 `fitScale` 需要监听容器 resize 实时重算

DOM 三层嵌套结构（滚动容器 → 占位层 → 变换层）的设计是解决"缩放后滚动区域大小正确"这一问题的关键。

### 难点 5：动态表单的自动推断与手动覆盖平衡

属性面板需要为 28 种组件自动生成表单，但纯自动推断无法覆盖所有情况：
- `boolean` 类型可以自动推断为 Switch，但 `string` 类型可能需要 Input、Textarea、Select、Color Picker 等多种控件
- 中文标签（如 "是否显示图例"）无法从英文字段名（`showLegend`）自动推断
- 枚举值（如对齐方式 "左/居中/右"）需要人工声明
- 某些字段需要隐藏（如 `colorPalette` 数组不适合直接编辑）

解决方案是"自动推断 + hints 注解覆盖"的两层模式，但维护 hints 配置仍有心智负担。

### 难点 6：中文分块与语义检索质量

RAG 系统的文档分块面临中文特有的挑战：
- 中文没有天然的单词边界，`RecursiveCharacterTextSplitter` 按字符数切割可能把完整的 JSON 代码示例切断
- chunkSize=500 字符（约 250 汉字）的粒度需要在"语义完整性"和"检索精度"之间权衡
- 技术文档中混合了大量中英文和代码块，分块质量直接影响检索召回率

### 难点 7：多轮对话中的上下文管理

用户可能在同一页面上进行多轮 AI 对话（追加组件、修改组件、调整布局），上下文管理是核心难题：
- 对话历史会随轮次增长，token 消耗急剧膨胀 → 通过 `summarizationMiddleware` 自动压缩
- Agent 需要知道画布当前状态（已有哪些组件、它们的位置和属性） → 通过组件清单注入 prompt
- 修改已有组件时需要精准引用组件 ID → 通过 Upsert 语义和 ID 匹配机制
- 不同页面的对话必须隔离 → 通过 `thread_id` 路由到不同的 checkpointer 实例

---

## 五、工程化亮点

### 19. Claude Skill 驱动的组件开发规范

项目配置了自定义的 `create-component` Skill（`.claude/skills/create-component/skill.md`），标准化了新增物料组件的完整 7 步流程：

1. 定义类型（`types.ts` + `basic.ts`/`chart.ts`）
2. 创建默认配置（`basic-config/` 或 `chart-config/`）
3. 添加属性面板 hints
4. 创建 React 渲染组件
5. 注册到组件注册表
6. 添加默认配置映射
7. 添加到组件面板

确保每次新增组件都不会遗漏任何一环。

### 20. Zustand + Immer 的不可变状态管理

`schema-store` 使用 Zustand + Immer 中间件，对复杂嵌套的组件树进行状态管理：

- 直接修改 draft 对象（`state.schema.name = patch.name`），无需手写展开运算符
- 树操作（DFS 查找、递归删除）抽离为 `useTreeOperation`，与 store 解耦
- `gridSize` 在 `setSchema` 和 `updatePage` 中统一做 `normalizeGridSize` 规范化
- `selectedNode` 在 `setSelectedId` 和 `updateItem` 后自动同步，避免属性面板读到过时数据

### 21. 完善的文档体系

项目维护了多份设计文档，覆盖：
- 架构图（ASCII 图，6 大视图）
- 缩放系统详解
- 动态表单重构方案
- 组件维护指南
- Page API 文档
- AI 优化计划
- RAG 知识库文档（3 份）

---

## 六、亮点速查表（面试/简历用）

| 序号 | 亮点关键词 | 一句话描述 |
|------|-----------|-----------|
| 1 | Schema 驱动架构 | 一切皆 JSON DSL，编辑器操作 = 组件树 CRUD，渲染器递归实例化 |
| 2 | AI + SSE 实时生成 | LangChain Agent 逐组件流式推送，前端实时写入画布，支持精准修改 |
| 3 | 三层配置驱动表单 | 默认配置 + hints 注解 + 字段映射槽位，属性面板零硬编码 |
| 4 | 三重坐标系转换 | 视口坐标 - 画布偏移 + 滚动补偿 ÷ 缩放比例 → 网格吸附 |
| 5 | RAF 参考线防抖 | requestAnimationFrame 替代 setTimeout，拖拽时参考线帧同步更新 |
| 6 | 双模式缩放系统 | autoFit + manualZoom，三层 DOM 嵌套保证缩放后滚动正确 |
| 7 | 每组件一工具 | 28 个 LangChain Tool 各有独立 Zod Schema，降低 LLM 生成错误率 |
| 8 | 双模型 RAG | DeepSeek(生成) + 智谱(问答)，各取所长 |
| 9 | AbortGuard 中断控制 | 前端 ESC → AbortController → 后端 guard.throwIfAborted() 全链路取消 |
| 10 | 对话摘要压缩 | SummarizationMiddleware 自动摘要，解决多轮对话 token 膨胀 |
| 11 | 组件清单注入 | 每次 AI 请求注入画布现有组件 ID/类型，防止 AI 编造组件 ID |
| 12 | 生产者-消费者解耦 | AiEditInput → store → AIChatPanel，精准修改 UI 与 AI 通信完全解耦 |
| 13 | ECharts 统一封装 | EChartsBase + fieldMapping 抽象，14 种图表共享渲染/数据映射基础设施 |
| 14 | MongoDB 一库两用 | 业务数据库 + 向量数据库复用 MongoDB Atlas，减少基础设施复杂度 |
| 15 | Claude Skill 规范 | 7 步标准化流程确保新增组件不遗漏任何环节 |
