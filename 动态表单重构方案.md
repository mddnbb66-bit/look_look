# 动态表单重构方案

## 一、现状问题

### 1. 表单生成链路过重
- 当前链路：`*.config.ts`(默认值) + `*.meta.ts`(UI描述) → `to-json-schema`推导 → `FieldMeta`逐字段patch → 生成form-render JSON Schema
- `to-json-schema` 只能推断基础类型，几乎每个字段都要被 meta 覆盖，推导步骤是多余的中间层
- 每个组件要维护 config + meta 两个文件，字段必须手动对齐，新增字段容易遗漏 meta

### 2. dataSource 混在 props 中
- `dataSource`(复杂嵌套对象) 和视觉配置(简单类型) 混在同一个 `props` 里，阻碍 props 被自动推断渲染
- 字段映射(`xAxisField`/`yAxisField`/`nameField` 等) 本质是数据绑定，不是视觉配置，也混在 props 中

### 3. 字段映射体验差
- 用户需要手动输入字符串 field name（如输入 `"name"`, `"value"`），需要记住数据里有哪些 key
- 字段映射和数据分离在不同配置区域，修改数据后映射可能脱节
- 不同图表使用不同的映射字段名（`xAxisField`/`categoryField`/`nameField`），命名不统一

## 二、目标架构

```
PropertyPanel
  ├── PropsFormRenderer          ← 自动遍历 node.props，全是简单类型，零特殊处理
  │     ├── boolean  → Switch
  │     ├── number   → InputNumber
  │     ├── string   → hint.enum ? Select/Radio : Input
  │     └── 查 hints 注解表补中文标题和枚举
  │
  └── DataSourceEditor           ← 独立子组件（仅图表组件显示）
        ├── 来源切换 (static / api)
        ├── 数据编辑 (JSON editor / API URL + 刷新间隔)
        └── 字段映射 (从数据中自动提取可用字段，下拉选择)
```

## 三、DSL 结构调整

### 3.1 dataSource 从 props 提升到组件顶层

```ts
// 调整前
interface BarChartComponent {
    type: "BarChart";
    props: {
        title: string;
        xAxisField: string;     // 数据绑定，混在视觉配置里
        yAxisField: string;
        barColor?: string;
        dataSource?: ChartDataSource;  // 复杂嵌套，混在 props 里
    };
    style?: { ... };
}

// 调整后
interface BarChartComponent {
    type: "BarChart";
    props: {
        title: string;
        barColor?: string;      // 只剩纯视觉配置
    };
    dataSource?: ChartDataSource;   // 提升到顶层
    style?: { ... };
}
```

### 3.2 字段映射统一收入 dataSource

```ts
// 调整前
interface ChartDataSource {
    sourceType: "static" | "api";
    data: unknown[];
    apiUrl?: string;
    refreshInterval?: number;
}

// 调整后
interface ChartDataSource {
    sourceType: "static" | "api";
    // 静态数据
    data?: unknown[];
    // API 配置
    apiUrl?: string;
    refreshInterval?: number;
    // 字段映射（统一用语义化 key）
    fieldMapping?: Record<string, string>;
}
```

### 3.3 各图表声明自己需要的映射槽位

```ts
interface FieldSlot {
    key: string;      // 映射 key，如 "x", "y", "name", "value", "series"
    label: string;    // 中文标签，如 "X 轴(类目)"
    required?: boolean;
}

// 按图表类型定义
const chartFieldSlots: Partial<Record<ComponentType, FieldSlot[]>> = {
    BarChart:          [{ key: "x", label: "X 轴(类目)", required: true }, { key: "y", label: "Y 轴(数值)", required: true }],
    LineChart:         [{ key: "x", label: "X 轴", required: true },      { key: "y", label: "Y 轴", required: true }],
    PieChart:          [{ key: "name", label: "名称", required: true },   { key: "value", label: "数值", required: true }],
    MultiBarChart:     [{ key: "x", label: "X 轴" }, { key: "y", label: "Y 轴" }, { key: "series", label: "系列字段" }],
    BubbleChart:       [{ key: "x", label: "X 轴" }, { key: "y", label: "Y 轴" }, { key: "size", label: "气泡大小" }, { key: "category", label: "分组" }],
    HorizontalBarChart:[{ key: "category", label: "类目" }, { key: "value", label: "数值" }],
    FunnelChart:       [{ key: "name", label: "名称" }, { key: "value", label: "数值" }],
    TreemapChart:      [{ key: "name", label: "名称" }, { key: "value", label: "数值" }],
    // RadarChart: 走 indicators，不需要 fieldMapping
    // GaugeChart: 直接传 value，不需要 fieldMapping
};
```

### 3.4 图表渲染组件适配

```ts
// 调整前：从 props 读字段名
const { xAxisField, yAxisField, dataSource } = props;

// 调整后：从 node.dataSource.fieldMapping 读
const x = node.dataSource?.fieldMapping?.x ?? "name";
const y = node.dataSource?.fieldMapping?.y ?? "value";
```

## 四、表单渲染器设计

### 4.1 FormHint 注解接口

```ts
interface FormHint {
    title: string;                                    // 必须：中文标题
    enum?: (string | number)[];                       // 可选：枚举值
    widget?: "radio" | "select" | "textarea" | "hidden";  // 可选：控件覆盖
    min?: number;                                     // 可选：数字最小值
    max?: number;                                     // 可选：数字最大值
    placeholder?: string;                             // 可选：占位符
    description?: string;                             // 可选：辅助说明
    hidden?: boolean;                                 // 可选：隐藏该字段
}

type FormHints = Record<string, FormHint>;
```

### 4.2 自动推断规则

| props 值类型 | 推断控件 | 需要注解什么 |
|---|---|---|
| `boolean` | Switch | 只需 title |
| `number` | InputNumber | title，可选 min/max |
| 普通 `string` | Input | title，可选 placeholder |
| 有 enum 的 `string` | Select / Radio | title + enum |

### 4.3 示例对比

```ts
// 当前 badge.meta.ts（28行，需要写 widget）
{
    count:         { title: "展示数字",  widget: "inputNumber", minimum: 0 },
    overflowCount: { title: "封顶数字",  widget: "inputNumber", minimum: 1, description: "超出显示 xx+" },
    showZero:      { title: "零值显示",  widget: "switch", description: "值为 0 时是否显示" },
    dot:           { title: "小红点模式", widget: "switch" },
    color:         { title: "徽标颜色",  placeholder: "例如：#ff4d4f" },
    text:          { title: "正文内容",  placeholder: "请输入正文" },
    fontSize:      { title: "正文字号",  widget: "inputNumber", minimum: 12, maximum: 72 },
}

// 重构后 badge.hints.ts（精简，widget 由类型自动推断）
{
    count:         { title: "展示数字" },
    overflowCount: { title: "封顶数字", min: 1, description: "超出显示 xx+" },
    showZero:      { title: "零值显示" },
    dot:           { title: "小红点模式" },
    color:         { title: "徽标颜色" },
    text:          { title: "正文内容" },
    fontSize:      { title: "正文字号", min: 12, max: 72 },
}
```

## 五、任务清单

### 阶段一：DSL 结构调整（影响全链路，优先做）

- [x] **1.1** 修改 `ChartDataSource` 类型定义，增加 `fieldMapping` 字段 (`schema/types.ts`)
- [x] **1.2** 修改 14 个图表 props 接口，移除 `dataSource` 和字段映射字段 (`schema/chart.ts`)
- [x] **1.3** 修改 `BaseChartComponent`，增加顶层可选 `dataSource` 字段 (`schema/types.ts`)
- [x] **1.4** 修改 13 个图表 config 文件，将 `dataSource` + 字段映射从 `props` 挪到顶层 + `fieldMapping` (`chart-config/*.config.ts`)
- [x] **1.5** 修改 14 个图表渲染组件，从 `node.dataSource` 读取数据和 `fieldMapping` (`materials/chart/*.tsx`)
- [x] **1.6** 新建 `chartFieldSlots` 映射槽位配置 (`config/chart-field-slots.ts`)
- [x] **1.7** 同步修改后端 AI schema (`backend/src/ai/schema.js`)
- [x] **1.8** 同步修改后端 component tools (`backend/src/ai/tools/component-tools.js`)
- [x] **1.9** 同步修改后端 agent prompt，更新图表字段映射相关提示 (`backend/src/ai/prompt.js`)

### 阶段二：自建表单渲染器（前端核心重构）

- [ ] **2.1** 新建 `FormHint` 类型定义和 `PropsFormRenderer` 组件 (`components/panels/property-panel/PropsFormRenderer.tsx`)
- [ ] **2.2** 新建 `DataSourceEditor` 子组件，包含来源切换 + 数据编辑 + 字段映射下拉选择 (`components/panels/property-panel/DataSourceEditor.tsx`)
- [ ] **2.3** 新建 28 个 hints 注解文件 (`config/hints/*.hints.ts`)
- [ ] **2.4** 新建 hints 注册表 (`config/hints/registry.ts`)
- [ ] **2.5** 改写 `PropertyPanel/Index.tsx`，用 `PropsFormRenderer` + `DataSourceEditor` 替代 `FormRender`

### 阶段三：清理旧代码

- [ ] **3.1** 删除 28 个 `*.meta.ts` 文件 (`basic-meta/*.meta.ts` + `chart-meta/*.meta.ts`)
- [ ] **3.2** 删除 `basic-meta/common.ts`、`basic-meta/registry.ts`
- [ ] **3.3** 删除 `chart-meta/chart-common.ts`（DataSourceEditor 接管其职责）
- [ ] **3.4** 删除 `utils/formSchema.ts`
- [ ] **3.5** 删除 `types/to-json-schema.d.ts`
- [ ] **3.6** 卸载 `form-render` 和 `to-json-schema` 依赖

### 阶段四：验证

- [ ] **4.1** 基础组件属性面板功能验证（14 个基础组件）
- [ ] **4.2** 图表组件属性面板 + 数据配置面板功能验证（14 个图表组件）
- [ ] **4.3** AI 生成图表组件的 DSL 兼容性验证
- [ ] **4.4** 拖拽创建组件的默认配置验证
